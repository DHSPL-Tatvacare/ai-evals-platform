{
  "node_type": "source.cohort_query",
  "config": {
    "source_ref": "crm.lead_record",
    "payload_fields": ["first_name", "last_name"],
    "filters": [
      { "column": "stage", "op": "eq", "value": "qualified" },
      { "column": "score", "op": "gte", "value": 50 }
    ]
  }
}

// Scaffold placeholder — implemented by Track T1 (llm.extract node).
export function LlmExtractEditor(_props: {
  value: Record<string, unknown>;
  onChange: (next: Record<string, unknown>) => void;
}) {
  return (
    <p className="rounded-[var(--radius-default)] bg-[var(--bg-tertiary)] p-2 text-xs text-[var(--text-secondary)]">
      LLM extraction editor — implementation pending.
    </p>
  );
}

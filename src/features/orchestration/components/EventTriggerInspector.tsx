// Scaffold placeholder — implemented by Track T3 (event-trigger inspector + connect UI).
export function EventTriggerInspector() {
  return (
    <p className="rounded-[var(--radius-default)] bg-[var(--bg-tertiary)] p-2 text-xs text-[var(--text-secondary)]">
      Event trigger setup — implementation pending.
    </p>
  );
}

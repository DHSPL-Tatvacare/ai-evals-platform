/**
 * Placeholder freshness-badge action.
 *
 * Phase 0 ships this as a no-op. Phase 1+ replaces the body with the badge
 * extracted from `CrmListing.tsx` (last-sync timestamp + sync state).
 */
export function FreshnessBadgeAction() {
  return null;
}

export interface DbModel {
  model: string;
  table: string;
  keyColumns: string;
  description: string;
}

export const dbModels: DbModel[] = [
  { model: 'App', table: 'apps', keyColumns: 'id (UUID), slug (String), display_name (String), description (String), icon_url (String), is_active (Boolean), created_at (DateTime), updated_at (DateTime)', description: 'App model — registered applications available platform-wide' },
  { model: 'AuditLog', table: 'audit_log', keyColumns: 'id (UUID), tenant_id (UUID), actor_id (UUID), action (String), entity_type (String), entity_id (UUID), created_at (DateTime)', description: 'AuditLog model — immutable record of RBAC and user-management changes' },
  { model: 'ChatSession', table: 'chat_sessions', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), app_id (String), external_user_id (String ?), thread_id (String ?), server_session_id (String ?), last_response_id (String ?), title (String), status (String), is_first_message (Boolean)', description: 'Chat models - sessions and messages' },
  { model: 'ChatMessage', table: 'chat_messages', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), session_id (UUID), role (String), content (Text), metadata_ (JSON ?), status (String), error_message (Text ?), created_at (DateTime)', description: 'Chat models - sessions and messages' },
  { model: 'EvalRun', table: 'eval_runs', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), app_id (String), eval_type (String), listing_id (UUID ?), session_id (UUID ?), evaluator_id (UUID ?), job_id (UUID ?), status (String), error_message (Text ?), started_at (DateTime ?), completed_at (DateTime ?), duration_ms (Float ?), llm_provider (String ?), llm_model (String ?), config (JSON), result (JSON ?), summary (JSON ?), batch_metadata (JSON ?), created_at (DateTime)', description: 'Eval run models — single source of truth for ALL evaluations' },
  { model: 'ThreadEvaluation', table: 'thread_evaluations', keyColumns: 'id (Integer PK), run_id (UUID), thread_id (String), data_file_hash (String ?), intent_accuracy (Float ?), worst_correctness (String ?), efficiency_verdict (String ?), success_status (Boolean), result (JSON), created_at (DateTime)', description: 'Eval run models — single source of truth for ALL evaluations' },
  { model: 'AdversarialEvaluation', table: 'adversarial_evaluations', keyColumns: 'id (Integer PK), run_id (UUID), goal_flow (JSON ?), difficulty (String ?), active_traits (JSON ?), verdict (String ?), goal_achieved (Boolean), total_turns (Integer), result (JSON), created_at (DateTime)', description: 'Eval run models — single source of truth for ALL evaluations' },
  { model: 'ApiLog', table: 'api_logs', keyColumns: 'id (Integer PK), run_id (UUID ?), thread_id (String ?), test_case_label (String ?), provider (String), model (String), method (String), prompt (Text), system_prompt (Text ?), response (Text ?), error (Text ?), duration_ms (Float ?), tokens_in (Integer ?), tokens_out (Integer ?), created_at (DateTime)', description: 'Eval run models — single source of truth for ALL evaluations' },
  { model: 'EvaluationAnalytics', table: 'evaluation_analytics', keyColumns: 'id (UUID), tenant_id (UUID), app_id (String), scope (String), run_id (UUID ?), analytics_data (JSON), computed_at (DateTime), source_run_count (Integer ?), latest_source_run_at (DateTime ?)', description: 'Evaluation analytics model — unified caching for single-run reports and cross-run aggregates' },
  { model: 'Evaluator', table: 'evaluators', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), app_id (String), listing_id (UUID ?), name (String), prompt (Text), model_id (String ?), output_schema (JSON), is_global (Boolean), show_in_header (Boolean), forked_from (UUID ?)', description: 'Evaluator model - custom evaluator definitions' },
  { model: 'ExternalAgent', table: 'external_agents', keyColumns: 'id (UUID), tenant_id (UUID), source (String), external_id (String), name (String), email (String ?), extra_data (JSON ?)', description: 'External agent identity from CRM systems (LSQ, Salesforce, etc.)' },
  { model: 'FileRecord', table: 'files', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), original_name (String), mime_type (String ?), size_bytes (Integer ?), storage_path (String)', description: 'FileRecord model - file metadata (actual bytes on filesystem/blob storage)' },
  { model: 'History', table: 'history', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), app_id (String), entity_type (String ?), entity_id (String ?), source_type (String), source_id (String ?), status (String), duration_ms (Float ?), data (JSON ?), triggered_by (String), schema_version (String ?), user_context (JSON ?), timestamp (Integer)', description: 'History model - audit log for evaluator runs and events' },
  { model: 'InviteLink', table: 'invite_links', keyColumns: 'id (UUID), tenant_id (UUID), created_by (UUID), token_hash (String), role_id (UUID), uses_count (Integer), expires_at (DateTime), is_active (Boolean), created_at (DateTime)', description: 'InviteLink model — shareable signup links created by admins' },
  { model: 'Job', table: 'jobs', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), job_type (String), status (String), params (JSON), result (JSON ?), progress (JSON), error_message (Text ?), created_at (DateTime), started_at (DateTime ?), completed_at (DateTime ?)', description: 'Job model - background job queue for batch evaluations' },
  { model: 'Listing', table: 'listings', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), app_id (String), title (String), status (String), source_type (String), audio_file (JSON ?), transcript_file (JSON ?), structured_json_file (JSON ?), transcript (JSON ?), api_response (JSON ?), structured_output_references (JSON), structured_outputs (JSON)', description: 'Listing model - evaluation records' },
  { model: 'LsqLeadCache', table: 'lsq_lead_cache', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (UUID), prospect_id (String), first_name (String), last_name (String), phone (String), email (String), fetched_at (DateTime)', description: 'Cached LSQ lead data — lazily populated on detail view' },
  { model: 'Prompt', table: 'prompts', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (Integer PK), app_id (String), prompt_type (String), version (Integer), name (String), prompt (Text), description (Text), is_default (Boolean), source_type (String ?)', description: 'Prompt model - versioned LLM prompt templates' },
  { model: 'Role', table: 'roles', keyColumns: 'id (UUID), tenant_id (UUID), name (String), description (String), is_system (Boolean), created_at (DateTime), updated_at (DateTime)', description: 'Role models — RBAC roles, app access grants, and action permissions' },
  { model: 'RoleAppAccess', table: 'role_app_access', keyColumns: 'id (UUID), role_id (UUID), app_id (UUID), created_at (DateTime)', description: 'Role models — RBAC roles, app access grants, and action permissions' },
  { model: 'RolePermission', table: 'role_permissions', keyColumns: 'id (UUID), role_id (UUID), permission (String), created_at (DateTime)', description: 'Role models — RBAC roles, app access grants, and action permissions' },
  { model: 'Schema', table: 'schemas', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (Integer PK), app_id (String), prompt_type (String), version (Integer), name (String), schema_data (JSON), description (Text), is_default (Boolean), source_type (String ?)', description: 'Schema model - versioned JSON schemas for structured LLM output' },
  { model: 'Setting', table: 'settings', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (Integer PK), app_id (String ?), key (String), value (JSON), updated_at (DateTime)', description: 'Setting model - user/app configuration' },
  { model: 'Tag', table: 'tags', keyColumns: 'tenant_id (UUID FK→tenants.id), user_id (UUID FK→users.id), id (Integer PK), app_id (String), name (String), count (Integer), last_used (DateTime)', description: 'Tag model - tag registry for autocomplete' },
  { model: 'Tenant', table: 'tenants', keyColumns: 'id (UUID), name (String), slug (String), is_active (Boolean), created_at (DateTime), updated_at (DateTime)', description: 'Tenant model — top-level organization/account' },
  { model: 'TenantConfig', table: 'tenant_configs', keyColumns: 'id (UUID), tenant_id (UUID), app_url (String ?), logo_url (String ?), allowed_domains (JSON), created_at (DateTime), updated_at (DateTime)', description: 'TenantConfig model — per-tenant settings (URL, branding, domain restrictions)' },
  { model: 'User', table: 'users', keyColumns: 'id (UUID), tenant_id (UUID), email (String), password_hash (String), display_name (String), role_id (UUID), is_active (Boolean), created_at (DateTime), updated_at (DateTime)', description: 'User and RefreshToken models — authenticated users within a tenant' },
  { model: 'RefreshToken', table: 'refresh_tokens', keyColumns: 'id (UUID), user_id (UUID), token_hash (String), expires_at (DateTime), created_at (DateTime)', description: 'User and RefreshToken models — authenticated users within a tenant' },
];
